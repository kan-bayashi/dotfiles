language: ruby

services:
  - docker

before_install:
    - docker pull ubuntu:18.04
    - docker pull centos:centos7

script:
    - docker build docker/ubuntu/Dockerfile -t ubuntu
    - docker build docker/centos/Dockerfile -t centos
